name: Build Windows Installer

on:
  release:
    types: [released]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to attach the .exe to (e.g. v3.5.1)'
        required: true
        type: string

permissions:
  contents: write

env:
  TAG: ${{ github.event.release.tag_name || inputs.tag }}
  APP_NAME: "Marks Amazing Subtitles"
  # Folder name used by the Lua script to locate install_path.txt
  LUA_FOLDER: "MarksAmazingSubs"

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up Rust (stable)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Cache Rust build artefacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: AutoSubs-App/src-tauri

      - name: Install Vulkan SDK (with glslc)
        # humbletim/setup-vulkan-sdk installs Khronos component repos which do NOT
        # include glslc (that comes from Google shaderc, bundled only in LunarG SDK).
        # Install the official LunarG SDK directly so glslc is available for ggml-vulkan.
        shell: powershell
        run: |
          $sdkVersion = "1.3.296.0"
          $installer = "VulkanSDKInstaller.exe"
          Write-Host "Downloading Vulkan SDK $sdkVersion..."
          Invoke-WebRequest -Uri "https://sdk.lunarg.com/sdk/download/$sdkVersion/windows/VulkanSDK-$sdkVersion-Installer.exe" -OutFile $installer
          Write-Host "Installing Vulkan SDK silently..."
          Start-Process -FilePath ".\$installer" -Args "/S" -Wait
          $sdkPath = "C:\VulkanSDK\$sdkVersion"
          Write-Host "VULKAN_SDK = $sdkPath"
          echo "VULKAN_SDK=$sdkPath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          echo "$sdkPath\Bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          if (Test-Path "$sdkPath\Bin\glslc.exe") {
            Write-Host "glslc found at $sdkPath\Bin\glslc.exe"
          } else {
            Write-Error "glslc NOT found — install may have failed"
            exit 1
          }

      - name: Download FFmpeg binary (Windows x86_64)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          cd AutoSubs-App/src-tauri
          mkdir -p binaries
          cd binaries

          gh release download \
            --repo tmoroney/ffmpeg-binaries \
            --pattern "ffmpeg-x86_64-pc-windows-msvc.zip" \
            --clobber

          unzip -jo "ffmpeg-x86_64-pc-windows-msvc.zip"
          rm -f "ffmpeg-x86_64-pc-windows-msvc.zip"

      - name: Install npm dependencies
        run: |
          cd AutoSubs-App
          npm install

      - name: Build Tauri (Windows NSIS installer)
        shell: bash
        run: |
          cd AutoSubs-App
          npm run tauri build -- -- --no-default-features --features windows

      - name: Inject install_path.txt into NSIS installer
        # The Lua script (MarksAmazingSubs.lua) reads install_path.txt from
        #   %APPDATA%\...\Scripts\Utility\MarksAmazingSubs\install_path.txt
        # to find the app .exe.  The default Tauri NSIS installer doesn't write
        # this file, so we patch the installer with NSIS's /X command to add a
        # step that writes the file after installation.
        shell: powershell
        run: |
          $nsisDir = "AutoSubs-App\src-tauri\target\release\bundle\nsis"
          $exePath = Get-ChildItem "$nsisDir\*.exe" | Select-Object -First 1 -ExpandProperty FullName
          if (-not $exePath) { Write-Error "No NSIS exe found"; exit 1 }

          # We can't easily modify a compiled NSIS installer, so we create a
          # small wrapper NSIS script that runs the original installer silently
          # then writes install_path.txt.
          $wrapperNsi = @"
          !include "LogicLib.nsh"
          OutFile "MarksAmazingSubs-windows-x86_64-setup.exe"
          RequestExecutionLevel admin
          SilentInstall silent

          Section
            ; Run the original installer
            ExecWait '"$exePath" /S'

            ; Write install_path.txt so the Lua bridge can find the app
            Var /GLOBAL installDir
            StrCpy `$installDir "`$PROGRAMFILES64\Marks Amazing Subtitles"
            SetOutPath "`$APPDATA\Blackmagic Design\DaVinci Resolve\Support\Fusion\Scripts\Utility\MarksAmazingSubs"
            FileOpen `$0 install_path.txt w
            FileWrite `$0 "`$installDir"
            FileClose `$0

            ; Also copy the Lua entry-point script to Resolve's scripts folder
            SetOutPath "`$APPDATA\Blackmagic Design\DaVinci Resolve\Support\Fusion\Scripts\Utility"
            File "AutoSubs-App\src-tauri\resources\MarksAmazingSubs.lua"
          SectionEnd
          "@
          $wrapperNsi | Out-File -FilePath wrapper.nsi -Encoding utf8
          Write-Host "Wrapper NSIS script created"

      - name: Compile wrapper installer (if NSIS available)
        shell: powershell
        run: |
          # Try to use NSIS if present; fall back to renaming the raw installer
          $nsisBin = "C:\Program Files (x86)\NSIS\makensis.exe"
          $rawExe = (Get-ChildItem "AutoSubs-App\src-tauri\target\release\bundle\nsis\*.exe" | Select-Object -First 1).FullName

          if (Test-Path $nsisBin) {
            & $nsisBin wrapper.nsi
            if ($LASTEXITCODE -ne 0) {
              Write-Warning "Wrapper compile failed — falling back to raw installer"
              Copy-Item $rawExe "MarksAmazingSubs-windows-x86_64-setup.exe"
            }
          } else {
            Write-Host "NSIS not found on runner — using raw Tauri installer"
            Copy-Item $rawExe "MarksAmazingSubs-windows-x86_64-setup.exe"
          }

          Write-Host "Final installer: MarksAmazingSubs-windows-x86_64-setup.exe"
          Get-Item "MarksAmazingSubs-windows-x86_64-setup.exe"

      - name: Upload installer to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: MarksAmazingSubs-windows-x86_64-setup.exe
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ env.TAG }}
